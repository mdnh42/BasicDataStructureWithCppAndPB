#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin>>n;
    vector<int>a(n);

    for(int i=0; i<n;i++)
    {
        cin>>a[i];
    }

    vector<pair<int,int>>vp;


    for(int i=0; i<n; i++)
    {
        vp.push_back(vp)
    }
    sort(vp.begin(), vp(end()));

    for(auto it :vp)
    {
        cout<<it.first<<it.second<<" ";
    }
    return 0;
}

